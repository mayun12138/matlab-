%如果一个语句在一行内书写太长了，可能要另起一行接着写，在这种情况下我们需要在第一行末打上半个省略号（...），再开始第二行的书写。
x1=1+2+3+...
+4


%用 whos 命令将会产生一个在当前工作区内的所有变量和数组状况表，注意所有的变量在同一工作区，所以两个脚本文件(Script files)按顺序执行，第二个脚本文件(Script files)可以利用第一个脚本文件(Script files)所创建的变量。每一个变量和数组的内容可以通过在命令窗口(The Command Windows)中输入对应名字得到显示。

%可用 clear 命令删除在本工作区的变量
